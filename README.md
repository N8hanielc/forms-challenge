
---

## ✨ Features

### ✅ Frontend

- Built with **React**
- Fields: `Name`, `Email`, `Phone`
- **Real-time validation** (field-level and form-level)
- **Styled** layout with vertical alignment
- ✅ Unit tests (`App.test.js`) with `React Testing Library`

### ✅ Backend

- Built with **Express + MongoDB**
- Stores submitted people to `formdb.people` collection
- Connected via REST API:
  - `GET /api/people`
  - `POST /api/people`
- **Dockerized** for local development

---

## 🐳 Run Locally (Docker + npm)

### 1. Clone the Repo

```bash
git clone https://github.com/n8hanielc/forms-challenge.git
cd forms-challenge
